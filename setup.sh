#! /bin/bash

#set the time zone
timedatectl set-timezone  Africa/Accra

#set the RTC  to false
timedatectl set-local-rtc false

#set the system host name
#sudo hostnamectl set-hostname kiosk

#update the package
#sudo apt-get update

#upgrade the package
#sudo apt-get upgrade

#reboot the system
#sudo reboot
